on: push
name: main
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
    - id: test-js-action
      uses: weibeld/test-js-action@master
      with:
        who-to-greet: Daniel
    - name: get-output
      run: echo "The time was ${{ steps.test-js-action.outputs.time }}"
