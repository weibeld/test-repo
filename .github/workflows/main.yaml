on: push
name: main
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
    - name: install-yq
      uses: weibeld/install-yq@master
      with:
        version: 3.5.0
    - name: validate-yq-installation
      run: yq --version
