on: push
name: main
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
    - name: access-github-api
      run: |
        curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "$GITHUB_API_URL"/repos/mikefarah/yq/releases/latest | jq -r .tag_name
