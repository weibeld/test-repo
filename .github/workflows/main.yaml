on: push
name: main
jobs:
  main:
    runs-on: windows-latest
    steps:
    - uses: weibeld/install-yq@dev
      id: install-yq
    - run: echo "${{ steps.install-yq.outputs.commons }}"
    - run: yq --version
    - run: env | sort
