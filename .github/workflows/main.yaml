on: push
name: main
jobs:
  main:
    runs-on: windows-latest
    steps:
    - name: install-yq
      uses: weibeld/install-yq@master
      with:
        version: 3.3.0
    - name: validate-yq-installation
      run: yq --version
