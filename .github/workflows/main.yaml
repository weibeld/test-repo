on: push
name: main
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
    - name: install-yq
      uses: weibeld/install-yq@macos
      with:
        version: 3.3.0
    - name: validate-yq-installation
      run: yq --version
