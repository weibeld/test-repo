on: push
name: main
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
    - uses: weibeld/install-yq@dev
      id: install-yq
      with:
        version: 3.3.0
        github-token: ${{ github.token }}
